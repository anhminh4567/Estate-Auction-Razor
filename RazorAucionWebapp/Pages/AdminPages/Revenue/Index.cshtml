@page
@model RazorAucionWebapp.Pages.AdminPages.Revenue.IndexModel

@{
    ViewData["Title"] = "Index";
}

<style>
    .container {
       
    }

    .form-container {
        background-color: #f8f9fa;
        border: 1px solid #dee2e6;
        border-radius: 8px;
        padding: 20px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

        .container h1 {
            margin-bottom: 20px;
        }

        .container a {
            color: #007bff;
            text-decoration: none;
            margin-right: 10px;
        }

            .container a:hover {
                text-decoration: underline;
            }

        .container table {
            width: 100%;
        }

        .container th {
            background-color: darkslategrey;
            color: #ffffff;
        }

        .container td, .container th {
            padding: 12px;
            text-align: left;
            border: 1px solid #dee2e6;
        }

        .container tbody tr:nth-child(even) {
            background-color: #f2f2f2;
        }
</style>

<div class="container">
    <div class="form-container">
        <div style="text-align: center">
            <h1>Revenue View</h1>
        </div>
        <div class="total-commission-box">
            <h3>Total Commission</h3>
            <h1>@Model.TotalCommission.ToString("C")</h1>
        </div>
        <table class="table">
            <thead>
                <tr>
@*                     <th>
                        @Html.DisplayNameFor(model => model.AuctionReceipt[0].Auction)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.AuctionReceipt[0].Buyer)
                    </th> *@
                    <th>
                        @Html.DisplayNameFor(model => model.AuctionReceipt[0].AuctionId)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.AuctionReceipt[0].Amount)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.AuctionReceipt[0].RemainAmount)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.AuctionReceipt[0].Commission)
                    </th>
                    @*                 <th></th>
                    *@
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.AuctionReceipt)
                {
                    <tr>
@*                         <td>
                            @Html.DisplayFor(modelItem => item.Auction.AuctionId)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Buyer.CMND)
                        </td> *@
                        <td>
                            @Html.DisplayFor(modelItem => item.AuctionId)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Amount)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.RemainAmount)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Commission)
                        </td>
                        @* <td>
                    <a asp-page="./Edit" asp-route-id="@item.ReceiptId">Edit</a> |
                    <a asp-page="./Details" asp-route-id="@item.ReceiptId">Details</a> |
                    <a asp-page="./Delete" asp-route-id="@item.ReceiptId">Delete</a>
                    </td> *@
                    </tr>
                }
            </tbody>
        </table>
    </div>

</div>

